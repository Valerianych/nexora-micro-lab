import {pinReference as pin,connectionMarkup as connect,ledConnections,buttonConnections,sensorConnections} from './circuit-labels.js';
export const missions=[
 {name:'Первая цепь',short:'Светодиод',goal:'Светодиод включается и выключается каждые полсекунды.',code:`// Контакт, к которому подключён светодиод
int ledPin = 13;

void setup() {
  pinMode(ledPin, OUTPUT);
}

void loop() {
  digitalWrite(ledPin, HIGH);
  delay(500);
  digitalWrite(ledPin, LOW);
  delay(500);
}`,steps:[
 ['Знакомство с платой',`Arduino — маленький компьютер. Программа задаёт, что он делает. Сегодня используем ${pin('uno:13')} и землю ${pin('uno:GND.2')}. Нажми на название контакта здесь, чтобы найти его на схеме.`,'Плата уже получает питание от виртуального USB. Внешний светодиод пока ни к чему не подключён.'],
 ['Замкни цепь',`Для свечения нужна замкнутая цепь. На схеме нажми на начало и конец каждого провода:${connect(ledConnections)}Названия в инструкции можно нажать: они подсвечивают нужный контакт, но не добавляют провод.`,'На детали A (+) — анод, C (−) — катод. Резистор 220 Ом ограничивает ток. Его выводы 1 и 2 можно поменять местами.'],
 ['Оживи программу','Нажми «Запустить». В <code>setup()</code> контакт становится выходом. В <code>loop()</code> высокий уровень HIGH сменяется низким LOW. Между ними программа ждёт 500 миллисекунд.','Встроенная лампа L может мигать и без твоих проводов. Для миссии должен работать внешний светодиод.'],
 ['Проверь результат','Понаблюдай за внешним светодиодом несколько секунд. Затем нажми «Проверить миссию». Проверка смотрит на реальную работу цепи, а не только на текст программы.','Если лампа не загорелась, проверь оба её вывода, резистор и номер контакта в коде.']
 ]},
 {name:'Меняем ритм',short:'Переменные',goal:'Используй переменную pauseMs: 250 мс светится, 250 мс выключен.',code:`int ledPin = 13;
int pauseMs = 500;

void setup() {
  pinMode(ledPin, OUTPUT);
}

void loop() {
  digitalWrite(ledPin, HIGH);
  delay(pauseMs);
  digitalWrite(ledPin, LOW);
  delay(pauseMs);
}`,steps:[
 ['Дай значению имя','Переменная хранит значение, к которому можно обратиться по имени. <code>int pauseMs = 500;</code> создаёт переменную целого типа int с именем pauseMs и значением 500.','Имя выбирает программист. Здесь pauseMs означает «пауза в миллисекундах».'],
 ['Одно изменение — два места','В обоих вызовах <code>delay(pauseMs)</code> программа берёт значение одной переменной. Замени 500 в объявлении pauseMs на 250 и снова запусти схему.','Точка с запятой завершает команду. Сохрани её после числа.'],
 ['Проведи эксперимент','Попробуй значения 100 и 1000. Как изменился ритм? Теперь верни 250. Полный цикл включает две паузы: включено и выключено.','При pauseMs = 250 полный цикл занимает примерно 500 мс, то есть два мигания в секунду.'],
 ['Проверка без подсказки','Схема должна мигать с паузой 250 мс. Дай программе поработать и нажми «Проверить миссию».',`Если запишешь ledPin = 12, перенеси провод на ${pin('uno:12')}. Сейчас в программе ledPin = 13 — ему соответствует ${pin('uno:13')}.`]
 ]},
 {name:'Свет по нажатию',short:'Условия',goal:'Светодиод горит только пока кнопка нажата. Используй if/else.',code:`int ledPin = 13;
int buttonPin = 2;

void setup() {
  pinMode(ledPin, OUTPUT);
  pinMode(buttonPin, INPUT_PULLUP);
}

void loop() {
  bool pressed = digitalRead(buttonPin) == LOW;
  if (pressed) {
    digitalWrite(ledPin, LOW); // замени LOW
  } else {
    digitalWrite(ledPin, LOW);
  }
}`,steps:[
 ['Добавь кнопку',`Кнопка сообщает программе о нажатии. Цепь светодиода оставь. Добавь два провода:${connect(buttonConnections)}`,'На кнопке 1L и 1R — верхняя пара, 2L и 2R — нижняя. L — слева, R — справа. Контакты одной пары соединены внутри. Нажатие соединяет верхнюю и нижнюю пары.'],
 ['Прочитай вход','<code>INPUT_PULLUP</code> включает внутренний подтягивающий резистор: отпущенная кнопка даёт HIGH. Нажатая соединяет D2 с землёй и даёт LOW. <code>digitalRead</code> читает уровень.','bool хранит true или false. Сравнение == LOW возвращает true, когда вход имеет низкий уровень.'],
 ['Выбери действие','Условие <code>if (pressed)</code> выполняет первую ветку при нажатии. <code>else</code> — вторую, когда кнопка отпущена. Исправь команду в первой ветке, чтобы включать светодиод.','Используй HIGH для включения. LOW в ветке else оставь: отпущенная кнопка должна выключать свет.'],
 ['Испытай обе ветки','Запусти схему. Нажми и отпусти кнопку. Затем запусти проверку: мастерская сама проверит несколько нажатий на твоей схеме.','Проверка требует и реакции на кнопку, и выключения после отпускания. Постоянно горящий светодиод не подойдёт.']
 ]}
 ];

missions.push(
 {name:'Три коротких сигнала',short:'Цикл for',goal:'Три мигания по 200 мс, затем дополнительная пауза 1000 мс.',pattern:[.2,.2,.2,.2,.2,1.2],code:`int ledPin = 13;
void setup() {
  pinMode(ledPin, OUTPUT);
}
void loop() {
  for (int i = 0; i < 2; i++) {
    digitalWrite(ledPin, HIGH);
    delay(200);
    digitalWrite(ledPin, LOW);
    delay(200);
  }
  delay(1000);
}`,steps:[
 ['Проверь гипотезу','По улике сирена выдаёт две вспышки вместо трёх. Исправь условие <code>for</code>, чтобы вернуть правильное число повторений.','Внешняя функция loop() повторяет программу целиком. Внутри неё можно использовать цикл для отдельных действий.'],
 ['Как работает for','<code>int i = 0</code> — начинаем со счётчика 0. <code>i &lt; 2</code> — проверяем условие перед повторением. <code>i++</code> — прибавляем единицу после выполнения тела.','Сначала i = 0, затем i = 1. При i = 2 условие i < 2 ложно. Сейчас вспышек две.'],
 ['Измени число повторений','Исправь границу в условии цикла, чтобы получать три вспышки. Паузы внутри оставь по 200 мс, а после цикла — 1000 мс.','Счётчик начинается с нуля: для трёх повторений он принимает значения 0, 1 и 2.'],
 ['Проверь серию','Запусти программу. После трёх коротких вспышек должна быть длинная пауза. Дождись нескольких серий и проверь миссию.','Последнее выключение длится 1200 мс: 200 мс внутри цикла и ещё 1000 мс после него.']
 ]},
 {name:'Список световых сигналов',short:'Массивы',goal:'Вспышки 100, 250 и 500 мс с паузами 150 мс; после серии ещё 1000 мс.',pattern:[.1,.15,.25,.15,.5,1.15],code:`int ledPin = 13;
int durations[3] = {100, 250, 250};
void setup() {
  pinMode(ledPin, OUTPUT);
}
void loop() {
  for (int i = 0; i < 3; i++) {
    digitalWrite(ledPin, HIGH);
    delay(durations[i]);
    digitalWrite(ledPin, LOW);
    delay(150);
  }
  delay(1000);
}`,steps:[
 ['Сохрани несколько значений','Массив хранит несколько элементов. <code>int durations[3]</code> создаёт массив из трёх целых чисел. Значения в фигурных скобках задают его содержимое.','Каждое число в нашем массиве — длительность одной вспышки в миллисекундах.'],
 ['Найди элемент по индексу','Индексы начинаются с нуля. <code>durations[0]</code> — первое число, <code>durations[1]</code> — второе, <code>durations[2]</code> — третье.','Индекс 3 выходит за границы этого массива. У трёх элементов индексы 0, 1 и 2.'],
 ['Пусть цикл читает список','Выражение <code>durations[i]</code> берёт элемент с текущим индексом i. На каждом повторении цикла выбирается следующая длительность.','Измени только третий элемент массива, чтобы получить 100, 250 и 500 мс.'],
 ['Проверь новый ритм','Запусти программу. Вспышки должны становиться длиннее: короткая, средняя, длинная. После нескольких серий проверь миссию.','Попробуй поменять порядок элементов. Поведение меняется, а цикл остаётся тем же. Перед проверкой верни нужный порядок.']
 ]},
 {name:'Команда для устройства',short:'Функции',goal:'Функция blink создаёт вспышку; вызови её для 200 и 600 мс.',pattern:[.2,.2,.6,1.2],code:`int ledPin = 13;
void blink(int duration) {
  digitalWrite(ledPin, HIGH);
  delay(duration);
  digitalWrite(ledPin, LOW);
  delay(200);
}
void setup() {
  pinMode(ledPin, OUTPUT);
}
void loop() {
  blink(200);
  blink(200); // нужна вспышка 600 мс
  delay(1000);
}`,steps:[
 ['Дай действию имя','Функция объединяет команды в одно действие. <code>blink</code> включает свет, ждёт, выключает свет и снова ждёт. Теперь её можно вызывать по имени.','void означает, что эта функция не возвращает значение. Сегодня она выполняет действие.'],
 ['Передай параметр','<code>int duration</code> — параметр функции. При вызове <code>blink(200)</code> внутри функции duration становится равным 200. При другом вызове значение может быть другим.','Объявление функции само по себе не включает свет. Для выполнения нужен вызов.'],
 ['Используй функцию дважды','Первый вызов оставь <code>blink(200)</code>. Исправь второй, чтобы он создавал вспышку длительностью 600 мс.','Команды включения и выключения написаны только один раз. Различия мы передаём через параметр.'],
 ['Создай свой вариант','Проверь миссию после нескольких серий. Затем перейди в свободную мастерскую: попробуй вызывать blink в цикле или передавать числа из массива.','Ты уже можешь соединять переменные, условия, циклы, массивы и функции в одной программе.']
 ]}
);

missions.push(
 {name:'Охлаждение по температуре',short:'Датчик и порог',sensor:true,goal:'Светодиод показывает команду охлаждения: включён при 35 °C и выше, выключен ниже 35 °C.',code:`int ledPin = 13;
int sensorPin = A0;
int limitC = 45; // регламент требует другой порог

void setup() {
  pinMode(ledPin, OUTPUT);
}
void loop() {
  // TMP36: 0.5 В при 0 °C, затем +0.01 В на градус.
  float voltage = analogRead(sensorPin) * 5.0 / 1024.0;
  int temperature = (voltage - 0.5) * 100 + 0.5; // округление до градуса
  if (temperature >= limitC) {
    digitalWrite(ledPin, HIGH);
  } else {
    digitalWrite(ledPin, LOW);
  }
  delay(50);
}`,steps:[
 ['Подключи измерение',`Светодиод:${connect(ledConnections)}Датчик TMP36:${connect(sensorConnections)}`,'Светодиод обозначает команду охлаждения. Двигатель вентилятора в этой схеме не моделируется.'],
 ['От напряжения к числу','<code>analogRead(A0)</code> возвращает число от 0 до 1023. TMP36 меняет напряжение с температурой. Готовая формула переводит отсчёт АЦП в градусы с округлением.','float хранит дробное напряжение, int — целую температуру. Сейчас формулу менять не нужно.'],
 ['Исправь порог','Регламент требует охлаждения уже при <code>35 °C</code>. Измени limitC и сохрани сравнение <code>temperature >= limitC</code>.','>= включает равенство. Условие > 35 пропустит значение 35.'],
 ['Нагрей и остуди','Запусти программу. На ползунке датчика проверь 34, 35 и 40 °C. Затем верни 25. Нажми «Проверить миссию».','Автопроверка запускает твою прошивку на нескольких температурах. Она проверяет и включение, и выключение.']
 ]},
 {name:'Единый аварийный протокол',short:'Итоговое дело',sensor:true,pattern:[.1,.15,.25,.15,.5,1.15],goal:'От 35 °C ИЛИ по кнопке: вспышки 100, 250, 500 мс. В норме — выключено.',code:`int ledPin = 13;
int buttonPin = 2;
int sensorPin = A0;
int durations[3] = {100, 250, 250}; // проверь последнюю команду

void blink(int duration) {
  digitalWrite(ledPin, HIGH);
  delay(duration);
  digitalWrite(ledPin, LOW);
  delay(150);
}
void setup() {
  pinMode(ledPin, OUTPUT);
  pinMode(buttonPin, INPUT_PULLUP);
}
void loop() {
  float voltage = analogRead(sensorPin) * 5.0 / 1024.0;
  int temperature = (voltage - 0.5) * 100 + 0.5;
  bool pressed = digitalRead(buttonPin) == LOW;
  // Достаточно одной причины тревоги. Проверь связку:
  if (temperature >= 35 && pressed) {
    for (int i = 0; i < 3; i++) {
      blink(durations[i]);
    }
    delay(1000);
  } else {
    digitalWrite(ledPin, LOW);
  }
}`,steps:[
 ['Собери входы и выход',`Светодиод:${connect(ledConnections)}Кнопка:${connect(buttonConnections)}Датчик TMP36:${connect(sensorConnections)}`,'Кнопка и датчик — независимые входы. Светодиод показывает выход протокола.'],
 ['Выбери логическую связку','<code>&&</code> требует обе причины одновременно. <code>||</code> достаточно одной. По условию дела нужна тревога от перегрева ИЛИ кнопки.','Сначала проверь каждую причину отдельно, затем обе вместе.'],
 ['Проверь список и инструмент','В массиве нужны <code>{100, 250, 500}</code>. Цикл читает <code>durations[i]</code> и передаёт значение функции <code>blink</code>.','Функция описывает одну вспышку. Цикл собирает три вспышки в серию.'],
 ['Прими систему','Запусти код, проверь ползунок и кнопку, затем запусти проверку. Она испытает четыре сочетания входов и завершение тревоги.','В этой программе delay блокирует чтение входов до конца серии. После остывания и отпускания кнопки текущая серия закончится, следующая уже не начнётся.']
 ]}
);
